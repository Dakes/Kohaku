# Rust build output (Cargo.lock is committed: this is a binary)
/target/

# Static binaries staged for the image (just smoke, CI)
/dist/

# Nix
/result
/result-*
.direnv/

# Local runtime data and config (may hold secrets or reporter data)
/data/
/caddy/
*.db
*.db-wal
*.db-shm
*.db-journal
.env
.env.*
!.env.example
!.env.local.example

# Fuzzing
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/coverage/

# Downloaded third-party documentation (reference only, not ours)
/docs/vendor/

# Claude Code per-user settings (shared skills/commands stay tracked)
.claude/settings.local.json

# Editors / OS
.idea/
.vscode/
*.swp
*~
.DS_Store
